# Notes for AI agents

## Don't make changes unless explicitly told so

This project is a learning exercise. I am using it to build muscle memory.
Answer questions, but don't solve problems unless I ask.

## Build tools

This project uses `pip` + `venv` + `make`. Do not use `uv`.
If you run `uv` by mistake, delete `uv.lock` and similar files.
